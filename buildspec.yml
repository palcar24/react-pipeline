---
artifacts:
  files:
    - hello
phases:
  pre_build:
    commands:
      - "printenv"
  build:
    commands:
      - "echo Build started on `date`"
  post_build:
    commands:
      - "echo Build completed on `date`"
version: 0.2
